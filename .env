# Environment variables for Docker Compose

# Backend Configuration
NODE_ENV=development # Overridden to 'production' in backend service environment section for container, this is for local 'docker-compose up' context
PORT=3001
PULSECHAIN_RPC_URL=https://rpc.pulsechain.com
JWT_SECRET=THIS_IS_A_DEV_SECRET_REPLACE_IT_FOR_PRODUCTION_WITH_A_VERY_LONG_RANDOM_STRING_MIN_32_CHARS
JWT_EXPIRES_IN=1h

# Frontend (Nginx doesn't typically use these directly from here, but good for reference)
# Example: VITE_API_BASE_URL=http://localhost:3001/api
# (If frontend needs to know backend URL at build time, it would be set in frontend/Dockerfile ARG and passed to build command)
